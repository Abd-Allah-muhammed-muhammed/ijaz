<?php

/**
 * Known lazy-loading violation keys (Model::relation) still awaiting fix.
 * Shrink this list as eager-load / Resource guard fixes land. Empty = green.
 *
 * @return list<string>
 */
return [
];
